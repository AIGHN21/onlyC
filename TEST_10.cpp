#include<iostream>
#include<algorithm>
#include<map>
using namespace std;
#include<vector>
#define ll long long
int main() {
    ll n;
    cin>>n;
    vector<pair<ll,ll>> arr(n);
    for(ll i = 0 ; i < n ; i++){
        cin>>arr[i].first>>arr[i].second;
    }
    map<pair<ll,ll>,ll> mymap;
    for(ll i = 0 ; i < n ; i++){
        mymap
    }
    sort(arr.begin(),arr.end());
}