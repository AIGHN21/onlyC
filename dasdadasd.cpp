#include <iostream>
#include <vector>
#include <algorithm>
#define IO ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define ll long long
#define ln "\n"
using namespace std;
void Solve(){

}
int main(){
    IO
    ll n;
    for(ll i = 1 ; i<= n  ; i++}{
        for(ll j = 1 ; j <= m ;j++){
            cin>>arr[i][j];
        }
    }
    return 0;
}
