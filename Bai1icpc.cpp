#include <iostream>
#include <vector>
#include <algorithm>
#define IO ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define ll long long
#define ln "\n"
using namespace std;
void Solve(){
    ll n;
    cin>>n;
    vector<ll> arr(n+1);
    vector<ll> ts[n+1];
    ll j = 1;
    for(ll i = 1 ; i <= n ; i++){
        cin>>arr[i];
    }

}
int main(){
    IO
    int t; cin >> t;
    while(t--){
        Solve();
    }
    return 0;
}
