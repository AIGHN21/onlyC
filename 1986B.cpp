#include <bits/stdc++.h>
using namespace std;
void Solve(){
    int n,m;
    cin>>n>>m;
    vector<vector<int>> result(n,vector<int>(m));
    for(int i = 0 ; i < n ; i++){
        for(int j = 0 ; j < m ; j++){
            cin>>result[i][j];
        }
    }
    for(int i = 0 ; i < n ; i++){
         for(int j = 0 ; j < m ; j++){
            if(result[i])
         }
    }
}
int main(){
    int t;
    cin>>t;
    while(t--){
        Solve();
    }
}